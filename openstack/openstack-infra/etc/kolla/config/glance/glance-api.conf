# =============================================================================
# Glance API overrides
#
# glance_file_datadir_volume in globals.yml already points Glance to
# /var/lib/glance/images (the host-side NFS mount).
# Add only extra overrides here that are not covered by globals.yml.
# =============================================================================

[DEFAULT]
debug = False
